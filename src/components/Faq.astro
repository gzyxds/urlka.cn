---
const faqs = [
  {
    title: "172号卡分销系统",
    description: "全国三大运营商优质流量卡，更优惠的资费满足市场需求，支持官网APP查询，确保用户享受官方正品服务",
  },
  {
    title: "全网高佣",
    description: "所有号卡系列均为运营商直签一手资源，佣金全网最高！支持代理商自己购买卡品并获得佣金，同时可发展下级代理",
  },
  {
    title: "下级分销",
    description: "可邀请下级代理分销，下级推卡你也可以赚取佣金！通过设置佣金比例实现团队收益",
  },
  {
    title: "佣金保障",
    description: "平台所有代理佣金由平台统一结算，申请提现后由平台统一打款，避免上游代理跑路问题",
  },
  {
    title: "专属客服",
    description: "172号卡APP内置专属客服，及时解决推卡和售后问题，确保权益不受侵犯",
  },
  {
    title: "完善的分销系统",
    description: "系统提供订单管理、代理管理、财务管理等功能，方便代理商进行业务操作和管理",
  },
]
---

<!-- FAQ Section -->
<section class="bg-white dark:bg-dark py-12 lg:py-20">
  <div class="container mx-auto px-4">
    <div class="lg:grid lg:grid-cols-12 lg:gap-8">
      <!-- 左侧标题区域 -->
      <div class="lg:col-span-5 mb-8 lg:mb-0">
        <span class="block mb-2 text-lg font-semibold text-primary">常见问题</span>
        <h2 class="text-dark dark:text-white mb-4 text-2xl sm:text-3xl md:text-4xl font-bold leading-tight">
          为什么选择我们
        </h2>
        <p class="text-base text-body-color dark:text-dark-6">
          有更多问题？联系我们的
          <a href="/contact" class="font-semibold text-primary">客服团队</a>
          获取帮助。
        </p>
      </div>

      <!-- 右侧FAQ列表 -->
      <div class="lg:col-span-7">
        <div class="space-y-3">
          {
            faqs.map((faq, index) => (
              <div class="faq-item border border-gray-200 dark:border-dark-3 rounded-lg overflow-hidden">
                <button
                  type="button"
                  class="faq-trigger w-full flex items-center justify-between p-4 sm:p-5 text-left bg-white dark:bg-dark-2 focus:outline-none"
                  data-index={index}
                >
                  <span class="text-sm sm:text-base font-semibold text-dark dark:text-white pr-4">{faq.title}</span>
                  <svg class="faq-icon w-5 h-5 text-primary flex-shrink-0 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                  </svg>
                </button>
                <div class="faq-content hidden">
                  <div class="px-4 sm:px-5 pb-4 sm:pb-5 text-sm sm:text-base text-body-color dark:text-dark-6 leading-relaxed">
                    {faq.description}
                  </div>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const triggers = document.querySelectorAll('.faq-trigger');
    
    triggers.forEach(trigger => {
      trigger.addEventListener('click', () => {
        const item = trigger.closest('.faq-item');
        const content = item?.querySelector('.faq-content');
        const icon = trigger.querySelector('.faq-icon');
        
        if (content && icon) {
          const isOpen = !content.classList.contains('hidden');
          
          // 关闭所有其他展开的项
          document.querySelectorAll('.faq-item').forEach(otherItem => {
            if (otherItem !== item) {
              otherItem.querySelector('.faq-content')?.classList.add('hidden');
              otherItem.querySelector('.faq-icon')?.classList.remove('rotate-180');
            }
          });
          
          // 切换当前项
          content.classList.toggle('hidden');
          icon.classList.toggle('rotate-180');
        }
      });
    });
  });
</script>